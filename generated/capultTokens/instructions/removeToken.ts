/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi';
import { Serializer, bytes, mapSerializer, struct } from '@metaplex-foundation/umi/serializers';
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared';

// Accounts.
export type RemoveTokenInstructionAccounts = {
  tokenDetails: PublicKey | Pda;
  authority?: Signer;
  tokenMint: PublicKey | Pda;
};

// Data.
export type RemoveTokenInstructionData = { discriminator: Uint8Array };

export type RemoveTokenInstructionDataArgs = {};

export function getRemoveTokenInstructionDataSerializer(): Serializer<
  RemoveTokenInstructionDataArgs,
  RemoveTokenInstructionData
> {
  return mapSerializer<RemoveTokenInstructionDataArgs, any, RemoveTokenInstructionData>(
    struct<RemoveTokenInstructionData>([['discriminator', bytes({ size: 8 })]], {
      description: 'RemoveTokenInstructionData',
    }),
    (value) => ({ ...value, discriminator: new Uint8Array([149, 134, 57, 61, 136, 2, 144, 145]) })
  ) as Serializer<RemoveTokenInstructionDataArgs, RemoveTokenInstructionData>;
}

// Instruction.
export function removeToken(
  context: Pick<Context, 'identity' | 'programs'>,
  input: RemoveTokenInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('capultTokens', 'CPLT8dWFQ1VH4ZJkvqSrLLFFPtCcKDm4XJ51t4K4mEiN');

  // Accounts.
  const resolvedAccounts = {
    tokenDetails: { index: 0, isWritable: true as boolean, value: input.tokenDetails ?? null },
    authority: { index: 1, isWritable: true as boolean, value: input.authority ?? null },
    tokenMint: { index: 2, isWritable: false as boolean, value: input.tokenMint ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId);

  // Data.
  const data = getRemoveTokenInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }]);
}
