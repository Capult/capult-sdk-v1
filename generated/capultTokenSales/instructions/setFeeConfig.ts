/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi';
import { Serializer, bytes, mapSerializer, struct, u16, u64 } from '@metaplex-foundation/umi/serializers';
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared';

// Accounts.
export type SetFeeConfigInstructionAccounts = {
  feeConfig: PublicKey | Pda;
  newWithdrawAuthority: PublicKey | Pda;
  authority?: Signer;
};

// Data.
export type SetFeeConfigInstructionData = {
  discriminator: Uint8Array;
  createSaleFee: bigint;
  botTax: bigint;
  buyFeeBasisPoints: number;
  withdrawFeeBasisPoints: number;
};

export type SetFeeConfigInstructionDataArgs = {
  createSaleFee: number | bigint;
  botTax: number | bigint;
  buyFeeBasisPoints: number;
  withdrawFeeBasisPoints: number;
};

export function getSetFeeConfigInstructionDataSerializer(): Serializer<
  SetFeeConfigInstructionDataArgs,
  SetFeeConfigInstructionData
> {
  return mapSerializer<SetFeeConfigInstructionDataArgs, any, SetFeeConfigInstructionData>(
    struct<SetFeeConfigInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['createSaleFee', u64()],
        ['botTax', u64()],
        ['buyFeeBasisPoints', u16()],
        ['withdrawFeeBasisPoints', u16()],
      ],
      { description: 'SetFeeConfigInstructionData' }
    ),
    (value) => ({ ...value, discriminator: new Uint8Array([221, 222, 52, 206, 114, 198, 64, 91]) })
  ) as Serializer<SetFeeConfigInstructionDataArgs, SetFeeConfigInstructionData>;
}

// Args.
export type SetFeeConfigInstructionArgs = SetFeeConfigInstructionDataArgs;

// Instruction.
export function setFeeConfig(
  context: Pick<Context, 'identity' | 'programs'>,
  input: SetFeeConfigInstructionAccounts & SetFeeConfigInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('capultTokenSales', 'CPTSoDzrvBad8fW2DWRgXhb2R5pa8sVdBJvZhfhuyYKe');

  // Accounts.
  const resolvedAccounts = {
    feeConfig: { index: 0, isWritable: true as boolean, value: input.feeConfig ?? null },
    newWithdrawAuthority: { index: 1, isWritable: false as boolean, value: input.newWithdrawAuthority ?? null },
    authority: { index: 2, isWritable: false as boolean, value: input.authority ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetFeeConfigInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId);

  // Data.
  const data = getSetFeeConfigInstructionDataSerializer().serialize(resolvedArgs as SetFeeConfigInstructionDataArgs);

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }]);
}
