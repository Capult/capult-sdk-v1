/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  i64,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { ResolvedAccount, ResolvedAccountsWithIndices, expectPublicKey, getAccountMetasAndSigners } from '../shared';

// Accounts.
export type InitiateUnlockInstructionAccounts = {
  eternalVault: PublicKey | Pda;
  authority?: Signer;
  vaultAta?: PublicKey | Pda;
  tokenMint: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type InitiateUnlockInstructionData = {
  discriminator: Uint8Array;
  unlockAmount: bigint;
  decimals: number;
  unlockDelay: bigint;
};

export type InitiateUnlockInstructionDataArgs = {
  unlockAmount: number | bigint;
  decimals: number;
  unlockDelay: number | bigint;
};

export function getInitiateUnlockInstructionDataSerializer(): Serializer<
  InitiateUnlockInstructionDataArgs,
  InitiateUnlockInstructionData
> {
  return mapSerializer<InitiateUnlockInstructionDataArgs, any, InitiateUnlockInstructionData>(
    struct<InitiateUnlockInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['unlockAmount', u64()],
        ['decimals', u8()],
        ['unlockDelay', i64()],
      ],
      { description: 'InitiateUnlockInstructionData' }
    ),
    (value) => ({ ...value, discriminator: new Uint8Array([41, 118, 177, 198, 27, 237, 231, 69]) })
  ) as Serializer<InitiateUnlockInstructionDataArgs, InitiateUnlockInstructionData>;
}

// Args.
export type InitiateUnlockInstructionArgs = InitiateUnlockInstructionDataArgs;

// Instruction.
export function initiateUnlock(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: InitiateUnlockInstructionAccounts & InitiateUnlockInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'capultEternalVaults',
    'CPEVjv7pvzLceHN9auJhniU2y3divtY4PUaTvLEoxpbP'
  );

  // Accounts.
  const resolvedAccounts = {
    eternalVault: { index: 0, isWritable: true as boolean, value: input.eternalVault ?? null },
    authority: { index: 1, isWritable: true as boolean, value: input.authority ?? null },
    vaultAta: { index: 2, isWritable: false as boolean, value: input.vaultAta ?? null },
    tokenMint: { index: 3, isWritable: false as boolean, value: input.tokenMint ?? null },
    tokenProgram: { index: 4, isWritable: false as boolean, value: input.tokenProgram ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitiateUnlockInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.vaultAta.value) {
    resolvedAccounts.vaultAta.value = context.eddsa.findPda(programId, [
      publicKeySerializer().serialize(expectPublicKey(resolvedAccounts.eternalVault.value)),
      publicKeySerializer().serialize(expectPublicKey(resolvedAccounts.tokenProgram.value)),
      publicKeySerializer().serialize(expectPublicKey(resolvedAccounts.tokenMint.value)),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId);

  // Data.
  const data = getInitiateUnlockInstructionDataSerializer().serialize(
    resolvedArgs as InitiateUnlockInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }]);
}
