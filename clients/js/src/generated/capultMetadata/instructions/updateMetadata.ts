/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi';
import { Serializer, bytes, mapSerializer, string, struct } from '@metaplex-foundation/umi/serializers';
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared';

// Accounts.
export type UpdateMetadataInstructionAccounts = {
  metadata: PublicKey | Pda;
  capultOwnedPda: PublicKey | Pda;
  authority?: Signer;
  payer?: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type UpdateMetadataInstructionData = { discriminator: Uint8Array; newUrl: string };

export type UpdateMetadataInstructionDataArgs = { newUrl: string };

export function getUpdateMetadataInstructionDataSerializer(): Serializer<
  UpdateMetadataInstructionDataArgs,
  UpdateMetadataInstructionData
> {
  return mapSerializer<UpdateMetadataInstructionDataArgs, any, UpdateMetadataInstructionData>(
    struct<UpdateMetadataInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['newUrl', string()],
      ],
      { description: 'UpdateMetadataInstructionData' }
    ),
    (value) => ({ ...value, discriminator: new Uint8Array([170, 182, 43, 239, 97, 78, 225, 186]) })
  ) as Serializer<UpdateMetadataInstructionDataArgs, UpdateMetadataInstructionData>;
}

// Args.
export type UpdateMetadataInstructionArgs = UpdateMetadataInstructionDataArgs;

// Instruction.
export function updateMetadata(
  context: Pick<Context, 'identity' | 'payer' | 'programs'>,
  input: UpdateMetadataInstructionAccounts & UpdateMetadataInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('capultMetadata', 'CPMDk1zycejhBAPLUiCrYfQWDD5Kdi19zzM4s1ts6EkQ');

  // Accounts.
  const resolvedAccounts = {
    metadata: { index: 0, isWritable: true as boolean, value: input.metadata ?? null },
    capultOwnedPda: { index: 1, isWritable: false as boolean, value: input.capultOwnedPda ?? null },
    authority: { index: 2, isWritable: false as boolean, value: input.authority ?? null },
    payer: { index: 3, isWritable: true as boolean, value: input.payer ?? null },
    systemProgram: { index: 4, isWritable: false as boolean, value: input.systemProgram ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateMetadataInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId);

  // Data.
  const data = getUpdateMetadataInstructionDataSerializer().serialize(
    resolvedArgs as UpdateMetadataInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }]);
}
