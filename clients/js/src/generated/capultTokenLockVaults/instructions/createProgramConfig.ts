/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi';
import { Serializer, bytes, mapSerializer, struct, u64 } from '@metaplex-foundation/umi/serializers';
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared';

// Accounts.
export type CreateProgramConfigInstructionAccounts = {
  programConfig: PublicKey | Pda;
  authority?: Signer;
  program: PublicKey | Pda;
  programData: PublicKey | Pda;
  feeWithdrawAuthority: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateProgramConfigInstructionData = {
  discriminator: Uint8Array;
  createVaultFee: bigint;
  ataInitReward: bigint;
  autoClaimReward: bigint;
};

export type CreateProgramConfigInstructionDataArgs = {
  createVaultFee: number | bigint;
  ataInitReward: number | bigint;
  autoClaimReward: number | bigint;
};

export function getCreateProgramConfigInstructionDataSerializer(): Serializer<
  CreateProgramConfigInstructionDataArgs,
  CreateProgramConfigInstructionData
> {
  return mapSerializer<CreateProgramConfigInstructionDataArgs, any, CreateProgramConfigInstructionData>(
    struct<CreateProgramConfigInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['createVaultFee', u64()],
        ['ataInitReward', u64()],
        ['autoClaimReward', u64()],
      ],
      { description: 'CreateProgramConfigInstructionData' }
    ),
    (value) => ({ ...value, discriminator: new Uint8Array([117, 255, 92, 176, 162, 243, 244, 67]) })
  ) as Serializer<CreateProgramConfigInstructionDataArgs, CreateProgramConfigInstructionData>;
}

// Args.
export type CreateProgramConfigInstructionArgs = CreateProgramConfigInstructionDataArgs;

// Instruction.
export function createProgramConfig(
  context: Pick<Context, 'identity' | 'programs'>,
  input: CreateProgramConfigInstructionAccounts & CreateProgramConfigInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'capultTokenLockVaults',
    'CPTLVeSKEXbPNZ4WnHTTGBX4J2uV3ktv3YkL9i7wSPwC'
  );

  // Accounts.
  const resolvedAccounts = {
    programConfig: { index: 0, isWritable: true as boolean, value: input.programConfig ?? null },
    authority: { index: 1, isWritable: true as boolean, value: input.authority ?? null },
    program: { index: 2, isWritable: false as boolean, value: input.program ?? null },
    programData: { index: 3, isWritable: false as boolean, value: input.programData ?? null },
    feeWithdrawAuthority: { index: 4, isWritable: false as boolean, value: input.feeWithdrawAuthority ?? null },
    systemProgram: { index: 5, isWritable: false as boolean, value: input.systemProgram ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateProgramConfigInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId);

  // Data.
  const data = getCreateProgramConfigInstructionDataSerializer().serialize(
    resolvedArgs as CreateProgramConfigInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }]);
}
