/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { Serializer, bytes, mapSerializer, option, struct, u64, u8 } from '@metaplex-foundation/umi/serializers';
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared';

// Accounts.
export type ClaimTokensInstructionAccounts = {
  purchaseRecord: PublicKey | Pda;
  tokenSale: PublicKey | Pda;
  payer?: Signer;
  authority?: Signer;
  recipient: PublicKey | Pda;
  tokenSaleAta: PublicKey | Pda;
  recipientAta: PublicKey | Pda;
  tokenMint: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  associatedTokenProgram: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type ClaimTokensInstructionData = {
  discriminator: Uint8Array;
  tokenAmount: bigint;
  decimals: number;
  transferFeeAmount: Option<bigint>;
};

export type ClaimTokensInstructionDataArgs = {
  tokenAmount: number | bigint;
  decimals: number;
  transferFeeAmount: OptionOrNullable<number | bigint>;
};

export function getClaimTokensInstructionDataSerializer(): Serializer<
  ClaimTokensInstructionDataArgs,
  ClaimTokensInstructionData
> {
  return mapSerializer<ClaimTokensInstructionDataArgs, any, ClaimTokensInstructionData>(
    struct<ClaimTokensInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['tokenAmount', u64()],
        ['decimals', u8()],
        ['transferFeeAmount', option(u64())],
      ],
      { description: 'ClaimTokensInstructionData' }
    ),
    (value) => ({ ...value, discriminator: new Uint8Array([108, 216, 210, 231, 0, 212, 42, 64]) })
  ) as Serializer<ClaimTokensInstructionDataArgs, ClaimTokensInstructionData>;
}

// Args.
export type ClaimTokensInstructionArgs = ClaimTokensInstructionDataArgs;

// Instruction.
export function claimTokens(
  context: Pick<Context, 'identity' | 'payer' | 'programs'>,
  input: ClaimTokensInstructionAccounts & ClaimTokensInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('capultTokenSales', 'CPTSoDzrvBad8fW2DWRgXhb2R5pa8sVdBJvZhfhuyYKe');

  // Accounts.
  const resolvedAccounts = {
    purchaseRecord: { index: 0, isWritable: true as boolean, value: input.purchaseRecord ?? null },
    tokenSale: { index: 1, isWritable: false as boolean, value: input.tokenSale ?? null },
    payer: { index: 2, isWritable: true as boolean, value: input.payer ?? null },
    authority: { index: 3, isWritable: false as boolean, value: input.authority ?? null },
    recipient: { index: 4, isWritable: false as boolean, value: input.recipient ?? null },
    tokenSaleAta: { index: 5, isWritable: true as boolean, value: input.tokenSaleAta ?? null },
    recipientAta: { index: 6, isWritable: true as boolean, value: input.recipientAta ?? null },
    tokenMint: { index: 7, isWritable: false as boolean, value: input.tokenMint ?? null },
    tokenProgram: { index: 8, isWritable: false as boolean, value: input.tokenProgram ?? null },
    associatedTokenProgram: { index: 9, isWritable: false as boolean, value: input.associatedTokenProgram ?? null },
    systemProgram: { index: 10, isWritable: false as boolean, value: input.systemProgram ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ClaimTokensInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId);

  // Data.
  const data = getClaimTokensInstructionDataSerializer().serialize(resolvedArgs as ClaimTokensInstructionDataArgs);

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }]);
}
